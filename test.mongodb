// use('social_network');
// db.users.drop()
// db.posts.drop()
// mongoimport --host=localhost --port=27017 --collection=users --db=social_network_db --file=data_generator/app/data/users.json
// mongoimport --host localhost --db social_network_db --collection posts --drop --file 'data_generator/app/data/posts.json'

// db.posts.insert({"name":"test"});

//author
//content
//shares
//likes, super ..
//date
//comments

// Run a find command to view items sold on April 4th, 2014.
// db.sales.find({ date: { $gte: new Date('2014-04-04'), $lt: new Date('2014-04-05') } });

// Run an aggregation to view total sales for each product in 2014.
// const aggregation = [
  // { $match: { date: { $gte: new Date('2014-01-01'), $lt: new Date('2015-01-01') } } },
  // { $group: { _id : '$item', totalSaleAmount: { $sum: { $multiply: [ '$price', '$quantity' ] } } } }
// ];
// db.sales.aggregate(aggregation);
